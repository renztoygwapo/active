/**
 * Object subscriptions widgets behavior
 */
jQuery.fn.objectSubscriptions = function(s) {
  var settings = jQuery.extend({ 
    'comments' : null
  }, s);
  
  return this.each(function() {
    var wrapper = $(this);
  });
  
};